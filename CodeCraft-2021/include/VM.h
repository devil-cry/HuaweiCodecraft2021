#ifndef VM_H
#define VM_H

#include<bits/stdc++.h>
#include "FastIO.h"
using namespace std;
using namespace FastIO;
class VM
{
public:
    int core,mem,isPair;

    void input()
    {
        read(core);read(mem);read(isPair);
    }

};

#endif // VM_H
